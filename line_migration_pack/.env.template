# LINE Messaging API設定
LINE_CHANNEL_ACCESS_TOKEN=    # LINE Developersで発行されるチャネルアクセストークン
LINE_CHANNEL_SECRET=          # LINE Developersで確認できるチャネルシークレット

# Webhook署名検証設定
SKIP_WEBHOOK_SIGNATURE_VERIFICATION=false    # 本番環境では必ずfalseに設定（開発環境のみtrue許可）

# Webhook設定（未実装・将来用）
WEBHOOK_EXTERNAL_URL=         # WebhookのパブリックURL（例: https://example.com/webhook）
WEBHOOK_PATH=/webhook         # Webhookのパス（デフォルト: /webhook）
APP_PORT=5001                 # Webhookサーバーのポート番号

# LINE公式アカウント情報（未実装・将来用）
LINE_BASIC_ID=                # LINE公式アカウントのID（例: @example）

# 注意事項:
# - 現在、.envとadmin/.envの2箇所で管理されています
# - admin/.envにはSKIP_WEBHOOK_SIGNATURE_VERIFICATIONのみ追加で設定されています
# - 本番環境では.envファイルの統合を推奨します